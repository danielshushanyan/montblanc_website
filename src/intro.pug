extends pug/base

append vars
	- title = 'Главная'

block content
	script.
		var element = document.getElementById("header");
		element.className = element.className.replace(/\bheader--loader\b/g, "");

		function getCookie(name) {
			var value = "; " + document.cookie;
			var parts = value.split("; " + name + "=");
			if (parts.length === 2) return parts.pop().split(";").shift();
		}

		if (getCookie('age') === 'true') {
			window.location.replace(`http://${window.location.host}/montblanc_website/`);
		}
	canvas#intro
	section.age
		.age__container
			h1.h1.age__title Взойти на вершину вкуса
			p.age__text Чтобы отправиться в путешествие с Mont Blanc, Вам должно быть больше 18 лет.
			.age__btn.js-age
				.age__btn-circle
				.js-catch.hover-s
					.js-follower
						.age__btn-square.js-square
						.age__btn-text.js-link Я старше 18 лет
